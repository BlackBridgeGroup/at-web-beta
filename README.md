# AlpenTalent Web App

One Next.js App Router site for marketing and the authenticated intake app. Run `npm install` then `npm run dev`; configure `.env` from `.env.example`, apply the Supabase migration, configure Google OAuth with only `openid email profile`, then deploy a Netlify preview before production.
